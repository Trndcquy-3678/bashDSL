# Project: A simple (deadass) DSL for bash and POSIX
> load [this file](./config.md.yml), as namespace `config`
## Summary
as the title said, we're coding a deadass simple DSL for `bash`
and POSIX shells like `busybox`'s `ash`, the overall goal is 
politely telling `bash` to cosplay as a real programming
language (:])
## Tech stack
- Python (stdlib only)
## Example (script template)
(read config.imports[3] :])
## Agent Config
(read config.imports[1] :])
## Current codebase structure (full breakdown)
read config.import[2]
